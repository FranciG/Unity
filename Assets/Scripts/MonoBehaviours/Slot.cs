using UnityEngine;
using UnityEngine.UI;
// Inherit from MonoBehaviour to attach the script to the Slot object
public class Slot : MonoBehaviour {
// reference to the Text inside the Slot to be set up on the unity editor
    public Text qtyText;
}
